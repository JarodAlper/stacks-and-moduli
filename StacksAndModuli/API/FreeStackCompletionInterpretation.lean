module

public import StacksAndModuli.API.FreeStackCompletionInterpretation.Relations

/-!
# Interpreting the free stack completion

Umbrella module for evaluating the formal syntax of `FreeStackCompletion` in an
arbitrary target stack and packaging the resulting extension functor.
-/

@[expose] public section
